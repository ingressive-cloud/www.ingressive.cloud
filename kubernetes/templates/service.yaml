apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingressive-www.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ingressive-www.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ingressive-www.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
